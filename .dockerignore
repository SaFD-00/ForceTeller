# 프론트엔드 (별도 배포)
web/

# Git
.git/
.gitignore

# 가상환경 — 이미지 안에서 uv sync로 다시 만든다. 호스트(.venv)를 굽으면
# 아키텍처가 다른 바이너리가 컨테이너 .venv를 덮어써 런타임이 깨진다.
.venv/

# Python 캐시
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# 환경 변수 (Railway에서 설정)
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# 로컬 DB (SQLite) — 이미지에 굽지 않음
*.db
*.sqlite
*.sqlite3

# 기타
*.md
!README.md
*.log
.DS_Store
Reference/
.claude/
